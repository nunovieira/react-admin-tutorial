# Reproduce an error with ra-data-graphql

```sh
npm install
npm start
```

Error in console

```log
Unhandled Promise Rejection: TypeError: buildQueryFactory is not a function. (In 'buildQueryFactory(introspectionResults, otherOptions)', 'buildQueryFactory' is undefined)
```
